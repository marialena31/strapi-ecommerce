{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "Email.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/",
      "handler": "Email.index",
      "config": {
        "policies": []
      }
    }
  ]
}
